# Build output — regenerated by Maven, never commit
target/
*.class
*.jar

# Secrets — the OpenRouter key must only ever live in the shell env
.env
*.env
secrets.properties
KEYS_AND_CREDENTIALS/

# Local one-off database inspection utility
db_schema_probe.java

# Derived data — regenerated from the API on demand
embeddings-cache.json
summaries-cache.json
knowledge-base.json
extractions-cache.json
entities.json

# Source data — supplied by Ori, not ours to version. The enriched export carries a
# 1536-float vector per post (~100 MB), and it holds real names and real posts.
post-clustering/data/

# Node / React (Phase 2)
node_modules/
dist/
build/

# Editor / OS
.idea/
.vscode/
*.iml
.DS_Store
